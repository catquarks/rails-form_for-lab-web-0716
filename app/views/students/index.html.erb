<p id="notice"><%= notice %></p>

<h1>All Students</h1>

<ul>
<% @students.each do |student| %>
  <li><%= link_to student.to_s, student_path(student) %></li>

<% end %>
</ul>

<%= link_to 'Create new student', new_student_path %>
